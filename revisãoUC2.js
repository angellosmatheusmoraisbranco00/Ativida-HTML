// ALGORITMO: calcular a média de 3 notas
// Passo 1 — receber as notas
const nota1 = 7.5;
const nota2 = 8.0;
const nota3 = 6.5;

// Passo 2 — somar as notas
const soma = nota1 + nota2 + nota3;

// Passo 3 — dividir pela quantidade de notas
const quantidade = 3;
const media = soma / quantidade;

// Passo 4 — exibir o resultado
console.log("media;", media); // Saída: Média: 7.333...
;

